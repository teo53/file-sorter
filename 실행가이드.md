# 🚀 파일 정리 시스템 실행 가이드

## 📋 목차
1. [빠른 시작 (Python만)](#빠른-시작)
2. [전체 시스템 실행 (대시보드 포함)](#전체-시스템-실행)
3. [자동화 설정](#자동화-설정)

---

## 1️⃣ 빠른 시작 (Python만)

가장 간단한 방법입니다. 대시보드 없이 파일만 정리합니다.

### 단계 1: 프로젝트 폴더로 이동
```powershell
cd "c:\Users\mapdr\Desktop\폴더 정리툴\src"
```

### 단계 2: 테스트 모드 실행 (권장)
실제로 파일을 이동하지 않고 미리보기만 합니다.
```powershell
python main.py --dry-run
```

**결과**: 
- Desktop 파일 스캔
- 어떤 파일이 어디로 이동될지 미리보기
- 실제 이동은 하지 않음 ✅ 안전!

### 단계 3: 실제 파일 정리 실행
테스트 결과가 만족스럽다면 실제 실행합니다.
```powershell
python main.py
```

**실행 흐름**:
```
1. Desktop 스캔 중...
   → 151개 파일 발견

2. "151개 파일을 정리하시겠습니까? (y/n):" 
   → y 입력

3. 파일 처리 시작
   [File 1/151] screenshot_test.png
     [CATEGORY] images/screenshots
     [RENAME] screenshots_test_2024-12-04.png
     [MOVED] Desktop/images/screenshots/...
   
   ...

4. 작업 완료!
   [SUCCESS] 151 files
   [FAILED] 0 files
   [LOG] movement_log.json
```

### 옵션 설명
```powershell
# OCR 없이 실행 (더 빠름)
python main.py --no-ocr

# 테스트 모드 + OCR 없이
python main.py --dry-run --no-ocr

# 도움말 보기
python main.py --help
```

---

## 2️⃣ 전체 시스템 실행 (대시보드 포함)

웹 대시보드로 파일 이동 기록을 확인하고 롤백할 수 있습니다.

### 준비: 대시보드 설치 (최초 1회만)
```powershell
cd "c:\Users\mapdr\Desktop\폴더 정리툴\dashboard"
npm install
```
⏱️ 약 2-3분 소요

### 실행 방법

**3개의 PowerShell 창을 엽니다:**

#### 🪟 터미널 1: FastAPI 서버 시작
```powershell
cd "c:\Users\mapdr\Desktop\폴더 정리툴\api"
python main.py
```

**출력**:
```
============================================================
[SERVER] FastAPI Server Started
============================================================
Address: http://localhost:8000
API Docs: http://localhost:8000/docs
============================================================
```

✅ **이 창은 계속 열어두세요!**

---

#### 🪟 터미널 2: Next.js 대시보드 시작
```powershell
cd "c:\Users\mapdr\Desktop\폴더 정리툴\dashboard"
npm run dev
```

**출력**:
```
- ready started server on 0.0.0.0:3000
- Local:        http://localhost:3000
```

✅ **이 창도 계속 열어두세요!**

---

#### 🪟 터미널 3: 파일 정리 실행
```powershell
cd "c:\Users\mapdr\Desktop\폴더 정리툴\src"
python main.py
```

파일 정리가 완료되면 이 창은 닫아도 됩니다.

---

#### 🌐 브라우저: 대시보드 접속

1. 브라우저 열기
2. 주소창에 입력: `http://localhost:3000`

**대시보드 기능**:
- 📊 통계 카드: 총 파일 수, 롤백 수, 카테고리 개수
- 🔍 필터: 카테고리별, 날짜별, 키워드 검색
- 📋 로그 테이블: 모든 파일 이동 기록
- ↩️ 롤백 버튼: 클릭 한 번으로 원래 위치로 복원

**사용 예시**:
```
1. 대시보드에서 "스크린샷" 카테고리 필터 선택
2. screenshot_test_2024.png 파일 찾기
3. "복원" 버튼 클릭
4. 파일이 Desktop으로 돌아감!
```

---

## 3️⃣ 자동화 설정

### 방법 1: Windows 작업 스케줄러

#### 단계 1: 배치 파일 생성
`c:\Users\mapdr\Desktop\폴더 정리툴\run_sorter.bat` 파일을 만듭니다:

```batch
@echo off
cd /d "c:\Users\mapdr\Desktop\폴더 정리툴\src"
python main.py --no-ocr
pause
```

#### 단계 2: 작업 스케줄러 설정
1. Windows 검색: "작업 스케줄러"
2. "기본 작업 만들기" 클릭
3. 이름: "파일 정리 자동화"
4. 트리거: "매일" 또는 "컴퓨터 시작 시"
5. 작업: `c:\Users\mapdr\Desktop\폴더 정리툴\run_sorter.bat`
6. 완료!

---

### 방법 2: 빠른 실행 바로가기

#### Desktop에 바로가기 만들기
1. 바탕화면 우클릭 → 새로 만들기 → 바로 가기
2. 위치 입력:
```
C:\Windows\System32\cmd.exe /k "cd /d c:\Users\mapdr\Desktop\폴더 정리툴\src && python main.py"
```
3. 이름: "⚡ 파일 정리"
4. 완료!

**사용**: 바로가기 더블클릭만 하면 됩니다!

---

## 🔧 문제 해결

### Q1: "python을 찾을 수 없습니다" 오류
```powershell
# Python 경로 확인
where python

# 전체 경로로 실행
C:\Users\mapdr\AppData\Local\Programs\Python\Python313\python.exe main.py
```

### Q2: FastAPI 서버가 안 켜져요
```powershell
# 의존성 다시 설치
cd api
pip install -r requirements.txt

# 포트 변경 (8000 포트가 사용 중인 경우)
# api/main.py 마지막 줄 수정:
uvicorn.run(app, host="0.0.0.0", port=8001)
```

### Q3: 대시보드가 안 열려요
```powershell
cd dashboard

# node_modules 삭제 후 재설치
Remove-Item -Recurse -Force node_modules
npm install

# 다시 실행
npm run dev
```

### Q4: Tesseract OCR 경고가 나와요
이것은 경고일 뿐 시스템은 정상 작동합니다.

**해결 방법** (선택사항):
`src/ocr_handler.py` 파일 31번째 줄 수정:
```python
pytesseract.pytesseract.tesseract_cmd = r'C:\Program Files\Tesseract-OCR\tesseract.exe'
```

---

## 📊 실행 시나리오 비교

| 시나리오 | 실행 시간 | 대시보드 | 롤백 | 추천 대상 |
|---------|----------|---------|------|----------|
| **빠른 시작** | 5초 | ❌ | ⚠️ 수동 | 간단히 정리만 |
| **전체 시스템** | 1분 | ✅ | ✅ | 완전한 관리 |
| **자동화** | 백그라운드 | 선택 | 선택 | 정기적 정리 |

---

## ✅ 권장 워크플로우

### 처음 사용할 때
```
1. 테스트 모드로 실행
   → python main.py --dry-run
   
2. 결과 확인 후 실제 실행
   → python main.py
   
3. 대시보드 확인 (선택)
   → API + Dashboard 실행
```

### 평소 사용
```
1. 바로가기 더블클릭
   → Desktop 파일 자동 정리
   
2. 필요시 대시보드에서 롤백
```

### 정기적 사용
```
1. 작업 스케줄러 설정
   → 매일 자동 실행
   
2. 주기적으로 로그 확인
   → movement_log.json
```

---

## 📝 핵심 명령어 요약

```powershell
# === 기본 사용 ===
cd "c:\Users\mapdr\Desktop\폴더 정리툴\src"
python main.py --dry-run    # 테스트
python main.py              # 실제 실행

# === 대시보드 사용 ===
# 터미널 1
cd api && python main.py

# 터미널 2  
cd dashboard && npm run dev

# 터미널 3
cd src && python main.py

# 브라우저: http://localhost:3000

# === 빠른 롤백 ===
cd src
python -c "from rollback import FileRollback; FileRollback().rollback_latest(1)"
```

---

## 🎯 다음 단계

1. ✅ **지금 바로**: `python main.py --dry-run` 실행해보기
2. ✅ **만족하면**: `python main.py`로 실제 정리
3. ✅ **필요하면**: 대시보드 설치 및 실행
4. ✅ **편하게**: 바로가기 만들기

**도움이 필요하면** `TEST_REPORT.md`와 `README.md`를 참고하세요!
