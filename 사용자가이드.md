# 사용자 가이드

## 🎯 시작하기

### 1. 설치

#### 방법 1: 자동 설치 (추천)
1. `🚀 자동설치.bat` 파일을 **우클릭** → **관리자 권한으로 실행**
2. 화면의 안내에 따라 설치 진행
3. 완료!

#### 방법 2: 수동 설치
```bash
# Python 설치 확인
python --version

# 의존성 설치
pip install -r requirements.txt

# 바로가기 생성
📌 바탕화면바로가기_생성.bat
```

---

## 📖 사용법

### 기본 사용 (GUI)

1. **바탕화면의 "📁 폴더선택_정리" 바로가기 실행**
2. 정리할 폴더 선택
3. 미리보기 확인
4. 'y' 입력하여 시작

### 명령어 사용

```bash
# 기본 실행 (폴더 선택 창이 나타남)
python src/main.py

# 특정 폴더 지정
python src/main.py --target "C:\Users\Downloads"

# 테스트 모드 (실제로 이동하지 않음)
python src/main.py --dry-run

# 자동 모드 (Desktop을 확인 없이 정리)
python src/main.py --auto
```

---

## ⚙️ 설정 변경

`config.json` 파일을 메모장으로 열어 수정하세요:

### 안전 설정

```json
"safety": {
  "large_file_threshold_mb": 100,    // 대용량 파일 기준 (MB)
  "max_files_warning": 500,          // 파일 수 경고 기준
  "exclude_extensions": [".exe", ".dll"],  // 제외할 확장자
  "protect_hidden_files": true       // 숨김 파일 보호
}
```

### 처리 설정

```json
"processing": {
  "enable_ocr": false,               // OCR 기능 (이미지 텍스트 추출)
  "show_progress": true,             // 진행 바 표시
  "preview_before_execute": true,    // 실행 전 미리보기
  "checkpoint_interval": 10          // 체크포인트 간격
}
```

### UI 설정

```json
"ui": {
  "use_emoji": true,                 // 이모지 사용
  "verbose_mode": false,             // 상세 모드
  "auto_open_log": false             // 완료 시 로그 자동 열기
}
```

---

## 🔍 기능 설명

### 1. 안전 장치

#### 대용량 파일 경고
- 100MB 이상 파일 발견 시 확인 요청
- 각 파일의 크기를 표시

#### 파일 수 제한
- 500개 이상 파일 처리 시 경고
- 배치 처리 권장

#### 중요 파일 보호
- 실행 파일 (.exe, .dll 등) 자동 제외
- 숨김 파일 보호 옵션
- 시스템 폴더 자동 제외

### 2. 진행 상황 표시

```
⏳ 진행: |████████████████████░░░░░| 80% 40/50
```

- 실시간 진행 바
- 처리된 파일 수 / 전체 파일 수
- 현재 처리 중인 파일 표시

### 3. 미리보기

실행 전 다음 정보를 확인:
- 처리될 파일 목록 (최대 10개 표시)
- 각 파일의 크기
- 총 파일 수와 전체 크기

### 4. 사용자 친화적 메시지

```
🤖 파일 자동 정리 시스템
📂 [1/5] 파일 스캔 중...
✓ 발견된 파일: 25개
🛡️ [2/5] 안전 검사 중...
✅ 안전 검사 통과: 25개 파일
👀 [3/5] 미리보기
```

---

## 📁 파일 분류 규칙

### 자동 분류

| 파일 종류 | 폴더 | 규칙 |
|----------|------|------|
| 스크린샷 | `images/screenshots/` | 파일명에 "캡처", "screenshot" 등 포함 |
| 제품 이미지 | `images/products/` | 파일명에 제품 키워드 포함 |
| 사진 | `images/photos/` | EXIF 카메라 정보가 있는 이미지 |
| 문서 | `documents/` | .pdf, .docx, .hwp, .xlsx 등 |
| 코드 | `project_files/` | .py, .js, .html 등 |
| 기타 | `etc/` | 위 규칙에 해당하지 않는 파일 |

### 파일명 형식

```
[카테고리]_[원본파일명].[확장자]

예시:
screenshots_스크린샷 2024-12-04.png
documents_보고서.pdf
products_유로폼 견적서.jpg
```

---

## 🔄 롤백 (되돌리기)

### 방법 1: 대시보드 사용
1. `🌐 대시보드_실행.bat` 실행
2. 브라우저에서 http://localhost:3000 접속
3. 로그 테이블에서 파일 찾기
4. "복원" 버튼 클릭

### 방법 2: 배치 파일 사용
```bash
ROLLBACK.bat
```

---

## ❓ 자주 묻는 질문 (FAQ)

### Q1. 파일이 삭제되나요?
**A.** 아니요! 파일은 절대 삭제되지 않고 **이동**만 합니다.

### Q2. 잘못 정리된 파일을 되돌릴 수 있나요?
**A.** 네! 모든 작업이 로그로 기록되며, 롤백 기능으로 원래 위치로 복원할 수 있습니다.

### Q3. OCR 기능이 필요한가요?
**A.** 선택사항입니다. 스크린샷에서 텍스트를 추출하려면 Tesseract OCR 설치가 필요하지만, 없어도 기본 파일 정리는 잘 작동합니다.

### Q4. 특정 파일을 정리에서 제외하려면?
**A.** `config.json`의 `exclude_extensions`에 파일 확장자를 추가하세요.

### Q5. 여러 폴더를 한 번에 정리할 수 있나요?
**A.** 현재는 한 번에 하나의 폴더만 가능합니다. 여러 폴더는 각각 실행해주세요.

### Q6. 하위 폴더의 파일도 정리되나요?
**A.** 기본값은 선택한 폴더만 정리합니다. 하위 폴더까지 포함하려면 코드에서 `recursive=True` 설정이 필요합니다.

---

## 🚨 문제 해결

### Python을 찾을 수 없음
```
'python'은(는) 내부 또는 외부 명령이 아닙니다.
```
**해결:** 
1. https://www.python.org/downloads/ 에서 Python 설치
2. 설치 시 "Add Python to PATH" 체크 필수

### 패키지 설치 오류
```
ERROR: Could not install packages
```
**해결:**
```bash
python -m pip install --upgrade pip
pip install -r requirements.txt
```

### 폴더 선택창이 나타나지 않음
**해결:**
- `config.json`에서 `preview_before_execute`를 `true`로 설정
- 또는 `--target` 옵션으로 폴더 직접 지정

---

## 💡 팁과 꿀팁

1. **테스트 먼저**: 처음 사용할 때는 `--dry-run` 옵션으로 테스트해보세요
2. **설정 백업**: `config.json`을 수정하기 전에 백업해두세요
3. **로그 확인**: `movement_log.json`에 모든 작업이 기록됩니다
4. **정기 정리**: 일주일에 한 번씩 실행하면 Desktop이 깔끔합니다!
5. **대용량 파일**: 동영상이나 설치 파일이 많으면 설정에서 제외하세요

---

## 📞 도움이 더 필요하신가요?

- 로그 파일 확인: `movement_log.json`
- 설정 파일 확인: `config.json`
- 테스트 모드 실행: `python src/main.py --dry-run`
